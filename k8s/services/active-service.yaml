apiVersion: v1
kind: Service
metadata:
  name: active
  namespace: deployment-engine
spec:
  type: NodePort
  selector:
    app: deployment-engine
    color: green
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
    - name: metrics
      port: 9113
      targetPort: 9113
