apiVersion: v1
kind: Service
metadata:
  name: active
  namespace: deployment-engine
spec:
  selector:
    app: deployment-engine
    color: green
  ports:
    - name: http
      port: 80
      targetPort: 80
